#!/bin/bash
var=`ls -Arst /var/opt/gitlab/backups | tail -n 2` #Get the file size in bytes
#echo $var
array=()
array+=($var)
arraylen="${#array[@]}" #length of the array


#echo $[array[-1]]
#echo $[array[-1]]

#echo $arraylen
for value in "${array[@]}" 
do
    echo $value
    #for value2 in "${array[@]}"
    #do
    #if [ "$value" == "$value2" ];then
    #newval=`ls -l /var/opt/gitlab/backups/ | grep "$value2"`
    #	echo "$newval"
    
#    if  
#    echo "TRUE $value $value2"

#    else
#    echo "FALSE $value $value2"
#   fi
   # done
done
